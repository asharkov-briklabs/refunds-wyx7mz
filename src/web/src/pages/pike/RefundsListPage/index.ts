import RefundsListPage from './RefundsListPage';

/**
 * Export file for the RefundsListPage component used in the Pike (merchant-facing) interface. 
 * This file re-exports the main component to simplify imports in other parts of the application, 
 * particularly for routing configuration.
 */
export default RefundsListPage;