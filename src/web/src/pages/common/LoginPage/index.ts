import LoginPage from './LoginPage'; // Import the LoginPage component from its file

/**
 * @file Exports the LoginPage component for authentication to both Pike (merchant) and Barracuda (admin) interfaces
 */

/**
 * @description Re-exports the LoginPage component as the default export from this index file
 * @exports LoginPage
 * @type {React.FC}
 */
export default LoginPage;