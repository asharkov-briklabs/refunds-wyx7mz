// LD1: Import the ApprovalQueue component for re-export
import ApprovalQueue from './ApprovalQueue';

// LD1: Expose the ApprovalQueue component for use in the Barracuda admin interface
export default ApprovalQueue;