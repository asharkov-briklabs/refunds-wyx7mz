import ReportGenerator, { ReportGeneratorProps } from './ReportGenerator'; // Import the main ReportGenerator component for export

/**
 * Exports the ReportGenerator component from the Barracuda admin interface, which provides
 * functionality for administrators to generate, customize, and export reports related to refund operations.
 */
export type { ReportGeneratorProps };

/**
 * Default export of the ReportGenerator component to enable clean imports from the directory
 */
export default ReportGenerator;