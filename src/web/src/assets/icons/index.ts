/**
 * Central icon export file for the Refunds Service application
 * 
 * This file consolidates all SVG icon components from separate files to provide
 * a single import point for all UI icons used throughout Pike and Barracuda interfaces.
 * 
 * Usage example:
 * import { CreateIcon, RefundsIcon, CheckCircleIcon } from '@/assets/icons';
 * 
 * @requires React 18.2+
 */

// Action icons (create, edit, delete, approve, etc.)
export * from './action-icons';

// Navigation icons (dashboard, transactions, refunds, etc.)
export * from './navigation-icons';

// Status icons (success, error, warning, pending, etc.)
export * from './status-icons';